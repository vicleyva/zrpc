import express from 'express'

const router = express.Router()
const usersController = null

// router.post("/users", (req, res, next) => usersController.createUser(req, res, next))
// router.put("/users", (req, res, next) => usersController.createUser(req, res, next))

export default router